#!/bin/bash
# PORT CONFIGURATION - Manage all app ports

# Format: [app_name]="host_port:container_port:expose_port"
declare -A APP_PORTS=(
    ["node-app"]="30080:8080:80"
    ["app-1"]="30081:3000:80" 
    ["app-2"]="30082:5000:80"
)

# Ngrok API ports for each app
declare -A NGROK_PORTS=(
    ["node-app"]="4041"
    ["app-1"]="4042"
)

# Port allocation range
RESERVED_PORTS_START=30080
RESERVED_PORTS_END=30100
